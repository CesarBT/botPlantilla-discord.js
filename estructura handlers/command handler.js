const Discord = require("discord.js")

module.exports = {
    name: "nombre del comando sin contar prefix",
    alias: ["si no vas a usar un alias saca este texto"],
    async run(client, message, args){
        //funcion que hara el comando
    }
}